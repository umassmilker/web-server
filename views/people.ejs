<!doctype html>
<html lang="en">
  <head>
    <link rel="stylesheet" href="/styles.css">
    <meta charset="UTF-8">
    <title><%= title %></title>
  </head>
  <body>
    <%- include('partials/header') %>
    <main>
      <ul>
        <% people.forEach(entry => { %>
          <li><%= entry.name %> - <%= entry.age %></li>
        <% }); %>
      </ul>
    </main>
  </body>
</html>
